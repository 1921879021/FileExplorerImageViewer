# ImagePeek 实现文档

> 在文件资源管理器中点击图片，右侧预览窗格立即显示。
> 支持 29 种格式（jpg/png/gif/bmp/tiff/ico/webp/avif/heic/jxl/svg/psd/exr/tga/pnm 等），
> 单文件便携 exe，发给别人双击即可启用。

---

## 1. 整体架构

```
┌────────────────────────────────────────────────────┐
│ ImagePeek.exe（单文件，11MB，WPF）                   │
│  ├─ 设置界面：一键启用/卸载、格式勾选、快速查看器      │
│  ├─ 载荷释放：内嵌 gzip 载荷 → %LocalAppData%        │
│  └─ AssemblyResolve 引导：exe 被单独复制也能运行      │
└──────────────────┬─────────────────────────────────┘
                   │ 共享
┌──────────────────▼─────────────────────────────────┐
│ ImagePeek.Core.dll（解码核心 + 注册逻辑）             │
│  L1 GDI+   ：jpg/png/gif/bmp/tiff/ico（最快）        │
│  L2 Magick ：webp/avif/heic/jxl/svg/psd/exr 兜底     │
│  进程内 LRU 缓存（6 张，重复点击零解码）               │
└──────────────────┬─────────────────────────────────┘
                   │ 共享
┌──────────────────▼─────────────────────────────────┐
│ ImagePeek.Preview.dll（COM 预览处理器）               │
│  IPreviewHandler + IInitializeWithFile + ...         │
│  专用 STA 渲染线程 + 消息泵                           │
│  运行于 prevhost.exe 代理进程（与 Explorer 隔离）      │
└────────────────────────────────────────────────────┘
```

**调用链**（点击图片时发生的事）：

```
Explorer 点击文件
  → SCM 读取 HKLM 注册表 → 启动 prevhost.exe（Medium IL 代理）
  → prevhost LoadLibrary("mscoree.dll") → CLR 垫片
  → 读版本化子键 CodeBase → 加载 ImagePeek.Preview.dll（托管）
  → IClassFactory → IInitializeWithFile.Initialize(路径)
  → IPreviewHandler.SetWindow / DoPreview
  → 专用 STA 线程创建渲染控件 → 后台线程解码 → UI 线程贴图
```

---

## 2. 目录结构

```
src/
├── ImagePeek.Core/          解码核心 + 注册表读写（net48 类库）
│   ├── DecodeCore.cs            两级解码 + LRU 缓存 + EXIF 方向
│   ├── NativeLoader.cs          原生 DLL 预加载（prevhost 环境必需）
│   ├── PreviewRegistration.cs   HKLM 注册/反注册（regasm 同款结构）
│   └── SupportedFormats.cs      格式清单
├── ImagePeek.Preview/       COM 预览处理器（net48，WinForms）
│   ├── Interop.cs               COM 接口声明（IID 与 SDK 一致）
│   ├── ImagePreviewHandler.cs   处理器实现 + 专用 STA 渲染线程
│   └── PreviewControl.cs        GDI+ 绘制（棋盘格/等比缩放/信息条）
├── ImagePeek/               主程序（net48 WPF）
│   ├── PayloadStore.cs          内嵌载荷释放（版本目录隔离）
│   ├── MainWindow.xaml(.cs)     设置 UI
│   ├── ViewerWindow.xaml(.cs)   快速查看器（←→切换/预加载/缩放）
│   └── pack-payload.ps1         构建时把 DLL 打包成 gzip 内嵌
└── ImagePeek.TestHost/      验证工具（COM 激活/代理渲染/像素检查）
build.ps1                     一键构建（依赖→打包→exe→测试工具）
```

---

## 3. 核心实现

### 3.1 COM 接口声明（Interop.cs）

IID 必须与 Windows SDK 完全一致（来源：MSDN《Writing Your Own Preview Handlers》）：

```csharp
[ComImport]
[Guid("8895b1c6-b41f-4c1c-a562-0d564250836f")]   // 注意：与 shellex 注册键同名
[InterfaceType(ComInterfaceType.InterfaceIsIUnknown)]
public interface IPreviewHandler
{
    [PreserveSig] int SetWindow(IntPtr hwnd, ref RECT rect);
    [PreserveSig] int SetRect(ref RECT rect);
    [PreserveSig] int DoPreview();
    [PreserveSig] int Unload();
    [PreserveSig] int SetFocus();
    [PreserveSig] int QueryFocus(out IntPtr phwnd);
    [PreserveSig] int TranslateAccelerator(ref MSG pmsg);
}

[ComImport]
[Guid("b7d14566-0509-4cce-a71f-0a554233bd9b")]
[InterfaceType(ComInterfaceType.InterfaceIsIUnknown)]
public interface IInitializeWithFile
{
    [PreserveSig] int Initialize([MarshalAs(UnmanagedType.LPWStr)] string pszFilePath, uint grfMode);
}

// 另需：IObjectWithSite {fc4801a3-2ba9-11cf-a229-00aa003d7352}
//       IOleWindow     {00000114-0000-0000-C000-000000000046}
//       IPreviewHandlerVisuals {8327b13c-b63f-4b24-9b8a-d010dcc3f599}（可选）
```

> 方法顺序 = vtable 顺序，不能乱。全部用 `[PreserveSig] int` 返回 HRESULT，
> 避免异常跨 COM 边界。

### 3.2 处理器类（ImagePreviewHandler.cs）

```csharp
[ComVisible(true)]
[Guid("A74E8F2C-6B3D-4F1A-9E5C-2D8B7A1F4E93")]   // 自己生成的固定 GUID
[ClassInterface(ClassInterfaceType.None)]
[ProgId("ImagePeek.PreviewHandler")]
public sealed class ImagePreviewHandler :
    IInitializeWithFile, IObjectWithSite, IOleWindow,
    IPreviewHandler, IPreviewHandlerVisuals
{
    private string _filePath;
    private PreviewControl _control;   // 由专用 STA 线程创建/拥有
    private Thread _uiThread;
    private CancellationTokenSource _cts;

    static ImagePreviewHandler()
    {
        // prevhost 的应用目录是 System32，托管依赖必须手动探测：
        // 在处理器自己所在目录里找 ImagePeek.Core.dll 等
        AppDomain.CurrentDomain.AssemblyResolve += (s, e) =>
        {
            string dir = Path.GetDirectoryName(e.RequestingAssembly.Location);
            string name = new AssemblyName(e.Name).Name + ".dll";
            string p = Path.Combine(dir, name);
            return File.Exists(p) ? Assembly.LoadFrom(p) : null;
        };
    }

    // Initialize 只存路径（规范要求），读取推迟到 DoPreview
    int IInitializeWithFile.Initialize(string pszFilePath, uint grfMode)
    { _filePath = pszFilePath; return 0; /* S_OK */ }

    int IPreviewHandler.DoPreview()
    {
        EnsureControl();                       // 确保 STA 渲染线程就绪
        RunOnUi(() => _control.SetMessage("正在加载…"));

        _cts = new CancellationTokenSource();
        var token = _cts.Token;
        string path = _filePath;

        Task.Run(() =>
        {
            try
            {
                var r = DecodeCore.Decode(path, DecodeCore.DefaultMaxPixels, token);
                RunOnUi(() => _control.SetImage(r.Bitmap, r.HasAlpha,
                    $"{r.Width} × {r.Height}  {r.Decoder}"));
            }
            catch (Exception ex)
            {
                RunOnUi(() => _control.SetMessage("无法预览该文件\n" + ex.Message));
            }
        }, token);
        return 0; // 立即返回，解码在后台，不阻塞预览窗格
    }
}
```

### 3.3 专用 STA 渲染线程（本项目最关键的坑）

**问题**：prevhost 的宿主线程是 MTA，而 WinForms 控件必须 STA，
否则窗口能创建但永远画不出内容（`OnPaint` 不触发）。
注册表的 `ThreadingModel=Apartment` 会被 mscoree 垫片绕过，不可靠。

**解决**：不依赖宿主线程，自建 STA 线程 + 独立消息泵：

```csharp
private void EnsureControl()
{
    if (_control != null) return;

    var ready = new ManualResetEventSlim(false);
    var parent = _parentHwnd;   // 宿主给的父窗口（预览窗格）
    var rect   = _rect;

    _uiThread = new Thread(() =>
    {
        var c = new PreviewControl();
        _control = c;
        NativeMethods.SetParent(c.Handle, parent);      // 挂进预览窗格
        c.SetBounds(rect.Left, rect.Top, rect.Right - rect.Left, rect.Bottom - rect.Top);
        c.Show();
        ready.Set();
        Application.Run();          // 专用消息泵，Unload 时 ExitThread
    });
    _uiThread.SetApartmentState(ApartmentState.STA);   // 强制 STA
    _uiThread.Start();
    ready.Wait(3000);
}

// 跨线程 UI 调用统一走这里
private void RunOnUi(Action action)
{
    try { _control?.BeginInvoke(action); } catch { }
}

int IPreviewHandler.Unload()
{
    _cts?.Cancel();
    var c = _control; _control = null;
    c?.BeginInvoke((Action)(() => Application.ExitThread()));  // 结束消息泵
    _uiThread?.Join(1000);
    return 0;
}
```

### 3.4 渲染控件（PreviewControl.cs）

```csharp
internal sealed class PreviewControl : Control
{
    // 双缓冲 + 自绘
    public PreviewControl()
    {
        SetStyle(ControlStyles.AllPaintingInWmPaint | ControlStyles.Opaque
               | ControlStyles.UserPaint | ControlStyles.ResizeRedraw
               | ControlStyles.DoubleBuffer, true);
    }

    protected override void OnPaint(PaintEventArgs e)
    {
        var g = e.Graphics;
        g.Clear(_bgColor);                       // 宿主指定的背景色
        if (_image != null)
        {
            Rectangle rect = FitRect(_image.Size, ClientSize);  // 等比包含
            if (_hasAlpha) DrawChecker(g, rect);     // 透明区画棋盘格
            g.InterpolationMode = InterpolationMode.HighQualityBicubic;
            g.DrawImage(_image, rect);
            DrawInfoBar(g);                          // 底部信息条：尺寸/大小/解码器
        }
        else TextRenderer.DrawText(g, _message, Font, ClientRectangle,
               Color.FromArgb(0x8A8A8A), TextFormatFlags.HorizontalCenter);
    }
}
```

### 3.5 两级解码核心（DecodeCore.cs）

```csharp
public static DecodeResult Decode(string path, int maxPixels, CancellationToken ct)
{
    // 1) 进程内 LRU 缓存命中 → 直接返回（连续点击零开销）
    //    key = 路径 + 文件大小 + mtime + maxPixels

    // 2) 按扩展名选择解码顺序
    if (SupportedFormats.IsVipsOnly(path))          // webp/avif/heic/jxl/svg/psd...
        return DecodeWithMagick(path, maxPixels);

    try   { return DecodeWithGdiPlus(path, maxPixels); }   // 常规格式走 L1
    catch { return DecodeWithMagick(path, maxPixels); }     // 失败走 L2 兜底
}

// L1：GDI+（注意 Bitmap(Stream) 要求流保持打开，需深拷贝解耦）
private static DecodeResult DecodeWithGdiPlus(string path, int maxPixels)
{
    using (var fs = new FileStream(path, FileMode.Open, FileAccess.Read, FileShare.Read))
    using (var loaded = new Bitmap(fs))
    {
        var current = ExifHelper.ApplyOrientation(loaded);   // EXIF 方向矫正
        var scaled  = DownscaleIfNeeded(current, maxPixels); // 超大图降采样
        return new DecodeResult { Bitmap = new Bitmap(scaled),   // 深拷贝
                                  HasAlpha = HasEffectiveAlpha(scaled),
                                  Decoder = "GDI+" };
    }
}

// L2：Magick.NET（100+ 格式兜底）
private static DecodeResult DecodeWithMagick(string path, int maxPixels)
{
    NativeLoader.EnsureLoaded();   // prevhost 里必须预加载原生 DLL（见 3.7）
    using (var mi = new ImageMagick.MagickImage())
    {
        mi.Read(path);
        mi.AutoOrient();
        mi.ColorSpace = ImageMagick.ColorSpace.sRGB;      // CMYK→sRGB
        if (mi.Width > maxPixels || mi.Height > maxPixels)
            mi.Resize((uint)maxPixels, (uint)maxPixels);  // 只缩不放
        bool alpha = mi.HasAlpha;
        mi.Format = ImageMagick.MagickFormat.Png;
        return new DecodeResult { Bitmap = new Bitmap(new MemoryStream(mi.ToByteArray())),
                                  HasAlpha = alpha, Decoder = "Magick" };
    }
}
```

### 3.6 注册表结构（PreviewRegistration.cs）—— 最容易翻车的地方

.NET Framework 托管 COM 的**版本化 InprocServer32 协议**（与官方
`regasm /codebase` 逐字节一致），且**只认 HKLM**（所以启用要一次 UAC）：

```
HKLM\Software\Classes\CLSID\{A74E8F2C-...}
    AppID      = {6D2B5079-2F0B-48DD-AB7F-97CEC514D30B}   ← 系统自带 prevhost 宿主
    DisableLowILProcessIsolation = 1                       ← 关键！退出 Low IL 沙箱
    InprocServer32
        (默认)          = mscoree.dll                        ← CLR 垫片，prevhost 加载它
        ThreadingModel  = Apartment
        \1.0.0.0                                            ← 版本号子键（协议核心）
            Class          = ImagePeek.Preview.ImagePreviewHandler
            Assembly       = ImagePeek.Preview, Version=1.0.0.0, ...
            RuntimeVersion = v4.0.30319
            CodeBase       = file:///C:/Users/x/AppData/Local/ImagePeek/runtime/<hash>/ImagePeek.Preview.dll

HKLM\Software\Classes\.png\shellex\{8895b1c6-b41f-4c1c-a562-0d564250836f}
    (默认) = {A74E8F2C-...}                                  ← 挂到扩展名

HKLM\Software\Microsoft\Windows\CurrentVersion\PreviewHandlers
    {A74E8F2C-...} = ImagePeek 图片预览处理器                  ← 处理器清单
```

```csharp
public static void RegisterHandler(string dllPath, string assemblyFullName)
{
    string version = new AssemblyName(assemblyFullName).Version.ToString();

    using (var key = Registry.LocalMachine.CreateSubKey(ClsidKey))
    {
        key.SetValue("AppID", AppIdPrevhost);
        key.SetValue("DisableLowILProcessIsolation", 1, RegistryValueKind.DWord);

        using (var ips = key.CreateSubKey("InprocServer32"))
        {
            ips.SetValue(null, "mscoree.dll");           // CLR 垫片
            ips.SetValue("ThreadingModel", "Apartment");
            using (var ver = ips.CreateSubKey(version))  // 版本化子键
            {
                ver.SetValue("Class", "ImagePeek.Preview.ImagePreviewHandler");
                ver.SetValue("Assembly", assemblyFullName);
                ver.SetValue("RuntimeVersion", "v4.0.30319");
                ver.SetValue("CodeBase", new Uri(dllPath).ToString());  // 注意！
            }
        }
        // ... ProgId 子键、Implemented Categories、PreviewHandlers 清单
    }
}
```

> **CodeBase 必须用 `new Uri(path).ToString()`（保留原始 Unicode 字符），
> 不能用 `AbsoluteUri`（百分号编码）** —— 后者会让 CLR 在 prevhost 里
> LoadFrom 失败（Fusion 日志报 0x80070002），这是排查最久的坑之一。

### 3.7 原生库预加载（NativeLoader.cs）

prevhost 的"应用目录"是 System32，`DllImport` 找不到我们目录里的
`Magick.Native-Q8-x64.dll`，必须先显式预加载：

```csharp
public static bool EnsureLoaded()
{
    string nativeDir = Path.Combine(解码核心所在目录, "native");
    foreach (string dll in Directory.EnumerateFiles(nativeDir, "*.dll"))
        LoadLibraryEx(dll, IntPtr.Zero, LOAD_WITH_ALTERED_SEARCH_PATH);
    // 之后托管层的 DllImport 按模块名命中已加载的 DLL
    return true;
}
```

### 3.8 单文件分发（PayloadStore.cs + pack-payload.ps1）

- **构建时**：把 `ImagePeek.Preview.dll`、`ImagePeek.Core.dll`、`Magick.NET*.dll`、
  原生库逐个 gzip 后作为 `EmbeddedResource` 打进 exe（约 11MB）。
- **运行时**：首次启用释放到版本化目录，避免 prevhost 占用 DLL 导致更新失败：

```
%LocalAppData%\ImagePeek\runtime\<内容哈希>\
    ImagePeek.Preview.dll / ImagePeek.Core.dll / Magick.NET*.dll
    native\Magick.Native-Q8-x64.dll
    complete.flag
```

- **便携引导**：exe 被单独复制到任何位置都能跑——`App` 静态构造函数挂
  `AssemblyResolve`，托管依赖缺失时自动释放载荷并从那里加载。

### 3.9 相邻预加载（ViewerWindow + DecodeCore 缓存）

- 预览处理器：进程内 LRU 缓存（6 张），prevhost 存活期间重复点击零解码。
- 快速查看器：当前图显示后，后台线程按顺序预解码前后各 2 张进同一缓存：

```csharp
private void PreloadNeighbors()
{
    foreach (int off in new[] { 1, -1, 2, -2 })
    {
        int i = _index + off;
        if (i >= 0 && i < _files.Length)
            DecodeCore.Decode(_files[i], ViewerMaxPixels);  // 命中缓存即秒开
    }
}
```

---

## 4. 踩坑记录（按排查代价排序）

| # | 坑 | 症状 | 原因 / 解决 |
|---|---|---|---|
| 1 | 托管 COM 注册写法 | Explorer 点击后**未响应** | InprocServer32 默认值不能写托管 DLL 路径（prevhost LoadLibrary 失败后无限等待）。必须 `mscoree.dll` + 版本化子键，且**只认 HKLM**（HKCU 写了没用）→ 启用需一次 UAC |
| 2 | CodeBase 百分号编码 | prevhost 内 0x80070002 | `Uri.AbsoluteUri` 把中文路径编码成 `%E8%B5%84...`，CLR LoadFrom 失败。用 `Uri.ToString()` 保留原始字符（regasm 同款） |
| 3 | Low IL 沙箱 | CLR 无法可靠初始化 | CLSID 上加 `DisableLowILProcessIsolation=1`，prevhost 以 Medium IL 运行（Excel 自家处理器同款） |
| 4 | **MTA 渲染线程** | 解码成功但**画面空白** | prevhost 宿主线程是 MTA，WinForms 画不出图；`ThreadingModel=Apartment` 会被 mscoree 垫片绕过。解决：**专用 STA 线程 + Application.Run 消息泵** |
| 5 | NetVips 新版包 | glib DLL 找不到 / 调用崩溃 | NetVips.Native 8.16+ 不再支持 .NET Framework；换 Magick.NET（原生/托管版本严格配对） |
| 6 | 原生 DLL 解析 | prevhost 里 Magick 加载失败 | 宿主应用目录是 System32；用 `LoadLibraryEx(完整路径, LOAD_WITH_ALTERED_SEARCH_PATH)` 预加载 |
| 7 | exe 单独复制闪退 | 0xe0434352 崩溃循环 | exe 旁边的托管依赖缺失；`AssemblyResolve` 引导 → 从内嵌载荷释放目录加载 |

**调试利器**：
- 处理器内写文件日志（`%LocalAppData%\ImagePeek\handler.log`）——prevhost 里没法挂调试器时全靠它
- Fusion 绑定日志（`HKLM\SOFTWARE\Microsoft\Fusion` ForceLog）——定位程序集加载失败
- TestHost 的 `sur` 命令：用 `CoCreateInstance(CLSCTX_LOCAL_SERVER)` 走与 Explorer
  完全相同的代理路径，并用 `CopyFromScreen` 做像素级渲染验证

---

## 5. 构建与分发

```
构建：powershell -File build.ps1          → 产物 src\ImagePeek\bin\Release\net48\ImagePeek.exe
交付：只发 ImagePeek.exe 一个文件（11MB）
使用：双击 → 一键启用（UAC 一次）→ 资源管理器按 Alt+P → 点击图片
卸载：ImagePeek.exe --disable（或界面"全部卸载"），删除 exe 即可
```

| 命令 | 作用 |
|---|---|
| `ImagePeek.exe --enable` | 启用（弹 UAC） |
| `ImagePeek.exe --disable` | 卸载注册 |
| `ImagePeek.exe --status` | 查看状态 |
| `ImagePeek.exe <图片路径>` | 打开快速查看器 |

## 6. 环境要求与已知限制

- Windows 10 1903+ / Win11，64 位，.NET Framework 4.8（系统自带）
- 启用需一次 UAC 管理员确认（托管 COM 版本化注册只认 HKLM 的系统限制）
- 动图显示第一帧；RAW 相机格式为后续计划
- 更新 exe 后需重新点一次"一键启用"（注册表 CodeBase 指向新版本目录）

## 7. 参考项目/文档

- 微软文档：Preview Handlers and Shell Preview Host / How to Register a Preview Handler / Building Preview Handlers
- microsoft/PowerToys `src/modules/previewpane`（工业级托管处理器参考）
- MSDN Magazine 2007：Stephen Toub《Writing Your Own Preview Handlers》（COM 接口 IID 权威来源）
- dlemstra/Magick.NET（Apache-2.0，100+ 格式解码）
- QL-Win/QuickLook（交互设计参考，GPL-3.0 未引用代码）
